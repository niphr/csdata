% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_variable_to_data_set.R
\name{add_granularity_geo_to_data_set}
\alias{add_granularity_geo_to_data_set}
\title{Add a granularity_geo column to a data set}
\usage{
add_granularity_geo_to_data_set(x, location_reference = NULL)
}
\arguments{
\item{x}{A data.table with a column named \code{location_code}. Only a
data.table method exists, so any other class raises "no applicable
method".}

\item{location_reference}{A data.table with columns \code{location_code} and
\code{granularity_geo} to use for lookup. When \code{NULL} (default), granularity
comes from the location code prefix (e.g. \code{"county_nor03"} -> \code{"county"}).
Its \code{location_code} values SHOULD be unique. A code that appears twice in
the reference produces more labels than \code{x} has rows. The assignment then
fails with "Supplied N items to be assigned to M items of column
'granularity_geo'". \code{\link[=nor_locations_names]{nor_locations_names()}} holds one such code today,
\code{"lab_nor084467"}, which two laboratories share.}
}
\value{
\code{x}, invisibly, with the \code{granularity_geo} column added or updated.
}
\description{
Derives the geographic granularity label from the \code{location_code} column.
Adds the label as a new \code{granularity_geo} column, and changes \code{x} in place.
When \code{location_reference} is \code{NULL}, the granularity comes from the location
code prefix. When you supply a reference table, the granularity comes from
that table.
}
\examples{
library(data.table)
data <- data.table(location_code = c("nation_nor", "county_nor03", "blah"))
csdata::add_granularity_geo_to_data_set(data)
print(data)

library(data.table)
data <- data.table(location_code = c("nation_nor", "county_nor03", "blah"))
csdata::add_granularity_geo_to_data_set(data, location_reference = csdata::nor_locations_names())
print(data)
}
\seealso{
\code{vignette("csdata", package = "csdata")}, which calls this
function on a worked example. \code{\link[=location_code_to_granularity_geo]{location_code_to_granularity_geo()}}
returns the same labels as a vector instead of writing them onto \code{x}.

Other data set column adders:
\code{\link[=add_iso3_to_data_set]{add_iso3_to_data_set()}}
}
\concept{data set column adders}
