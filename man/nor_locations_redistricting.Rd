% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nor_locations_redistricting.R
\name{nor_locations_redistricting}
\alias{nor_locations_redistricting}
\title{Redistricting weights for Norwegian geographic units}
\source{
Statistics Norway (SSB) municipal reform documentation.
}
\usage{
nor_locations_redistricting(border = csdata::config$border_nor)
}
\arguments{
\item{border}{Integer. The target geographic border year. Valid values:
\code{2024}. Defaults to \code{csdata::config$border_nor}.}
}
\value{
A data.table with columns:
\describe{
\item{location_code_current}{Location code under the target border year.
Each of the 421 distinct values also appears in
\code{\link[=nor_locations_names]{nor_locations_names()}}.}
\item{location_code_original}{Location code as it existed in \code{calyear}.
Of the 941 distinct values, 520 are retired codes that
\code{\link[=nor_locations_names]{nor_locations_names()}} no longer lists.}
\item{calyear}{The calendar year to which \code{location_code_original}
applies.}
\item{weighting}{Proportional weight to apply when you aggregate from
the original location to the current location. Values sum to 1 within
each \code{location_code_original} / \code{calyear} group, on all 40,819 of
those groups. A row spread across several current locations is
therefore split, not duplicated. Values need not sum to 1 within a
\code{location_code_current} / \code{calyear} group: 4,334 of those 35,160
groups do not.}
\item{granularity_geo}{Geographic granularity: one of \code{"nation"},
\code{"county"}, \code{"municip"}, \code{"wardbergen"}, \code{"wardoslo"},
\code{"wardstavanger"}, \code{"wardtrondheim"}, \code{"extrawardoslo"},
\code{"missingwardbergen"}, \code{"missingwardoslo"}, \code{"missingwardstavanger"},
\code{"missingwardtrondheim"}, \code{"notmainlandcounty"},
\code{"notmainlandmunicip"}, \code{"missingcounty"}, \code{"missingmunicip"}.}
}
}
\description{
Returns a data.table of weighting factors used to convert historical data
recorded under old administrative boundaries to the 2024 borders.
Each row maps an original location code (as of a given calendar year) to
the current location code, with a proportional weighting.
}
\examples{
d <- csdata::nor_locations_redistricting()
head(d)
d[calyear == 2019 & granularity_geo == "municip"]
}
\seealso{
No vignette covers this function.
\code{vignette("locations_norway", package = "csdata")} tabulates the
\code{location_code} values returned by \code{\link[=nor_locations_names]{nor_locations_names()}}, which are the
values \code{location_code_current} is drawn from.
}
