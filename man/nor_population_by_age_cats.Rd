% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nor_population_by_age_cats.R
\name{nor_population_by_age_cats}
\alias{nor_population_by_age_cats}
\title{Norwegian population aggregated into custom age categories}
\usage{
nor_population_by_age_cats(
  cats = NULL,
  include_total = TRUE,
  include_9999 = FALSE,
  border = csdata::config$border_nor
)
}
\arguments{
\item{cats}{A named or unnamed list of integer vectors that give the age
values to include in each category. Each vector element defines one age
band. If a list element is named, that name becomes the \code{age} label. If it
is not named, the label is \code{"LLL_HHH"}: the zero-padded lower and upper
bounds. Defaults to \code{NULL} (no custom bands; only \code{"total"} is
returned when \code{include_total = TRUE}).}

\item{include_total}{Logical. If \code{TRUE} (default), the function appends an
extra row group with \code{age = "total"} that covers all ages.}

\item{include_9999}{Logical. If \code{TRUE}, the function duplicates the most
recent calendar year and adds it with \code{calyear = 9999}. This follows the
cstidy convention for \code{granularity_time == "event_*"}. Default \code{FALSE}.}

\item{border}{Integer. The geographic border year. Valid values: \code{2024}.
Defaults to \code{csdata::config$border_nor}.}
}
\value{
A data.table with columns:
\describe{
\item{granularity_geo}{Geographic granularity level.}
\item{location_code}{Location code.}
\item{age}{Age category label, as derived from \code{cats} names or
auto-generated, plus \code{"total"} if \code{include_total = TRUE}.}
\item{sex}{Sex. Always \code{"total"}, because this function filters the
bundled dataset to the sex total. The dataset itself also holds
\code{"male"} and \code{"female"}; reach those with
\code{\link[=nor_population_by_sex_age_cats]{nor_population_by_sex_age_cats()}}.}
\item{calyear}{Calendar year.}
\item{pop_jan1_n}{Population count as of 1 January of \code{calyear}.}
\item{imputed}{Logical. \code{TRUE} if the value was imputed.}
}
}
\description{
Aggregates the bundled Norwegian population dataset (from Statistics Norway)
into caller-defined age bands. The underlying data covers every integer age
from 0 to 105 at national, georegion, county, municipality, and
city-district level. Not-mainland and unknown locations carry the single
placeholder age \code{-99} instead. \code{include_total} counts that age, and a band
such as \code{0:105} does not.
}
\examples{
# data.table's default multi-threading pushes this example past CRAN's
# CPU-to-elapsed limit, so pin it to one thread and restore afterwards.
old_threads <- data.table::getDTthreads()
data.table::setDTthreads(1)

# Default: return total population only
d <- nor_population_by_age_cats()
print(head(d[granularity_geo == "nation"]))

# Named age bands
d2 <- nor_population_by_age_cats(
  cats = list("children" = 0:17, "working_age" = 18:66, "elderly" = 67:105),
  include_total = TRUE
)
print(d2[granularity_geo == "nation" & calyear == 2024])

data.table::setDTthreads(old_threads)
}
\seealso{
\code{vignette("population_norway", package = "csdata")} plots the
national series and tabulates \code{pop_jan1_n} by location and calendar year
from this function. \code{vignette("csdata", package = "csdata")} gives the
csverse coding rules for the \code{age} and \code{sex} columns.

Other population data:
\code{\link[=nor_population_by_sex_age_cats]{nor_population_by_sex_age_cats()}}
}
\concept{population data}
