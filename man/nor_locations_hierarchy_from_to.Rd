% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nor_locations_hierarchy.R
\name{nor_locations_hierarchy_from_to}
\alias{nor_locations_hierarchy_from_to}
\title{Location hierarchies in Norway}
\usage{
nor_locations_hierarchy_from_to(
  from,
  to,
  include_to_name = FALSE,
  border = csdata::config$border_nor
)
}
\arguments{
\item{from}{Character vector. The source geographic granularity. One or more
of: \code{"wardoslo"}, \code{"extrawardoslo"}, \code{"wardbergen"}, \code{"wardtrondheim"},
\code{"wardstavanger"}, \code{"missingwardoslo"}, \code{"missingwardbergen"},
\code{"missingwardtrondheim"}, \code{"missingwardstavanger"}, \code{"municip"},
\code{"baregion"}, \code{"county"}, \code{"georegion"}, \code{"mtregion"},
\code{"notmainlandmunicip"}, \code{"notmainlandcounty"}, \code{"missingmunicip"},
\code{"missingcounty"}.}

\item{to}{Character vector. The target geographic granularity. Same valid
values as \code{from}.}

\item{include_to_name}{Logical. If \code{TRUE}, include the name of each \code{to}
location as a third column \code{to_name}. Default \code{FALSE}.}

\item{border}{Integer. The geographic border year. Valid values: \code{2024}.
Defaults to \code{csdata::config$border_nor}.}
}
\value{
A data.table with columns:
\describe{
\item{from_code}{Location code at the \code{from} granularity level.}
\item{to_code}{Location code at the \code{to} granularity level.}
\item{to_name}{Name of the \code{to} location (only present when
\code{include_to_name = TRUE}).}
}
The table has no key set, and a combination the bundled hierarchy cannot
express yields zero rows.
}
\description{
Returns a mapping between two geographic levels in Norway. Both \code{from} and
\code{to} accept character vectors. The function then returns every requested
combination, joined into a single data.table.
}
\details{
A combination that the bundled hierarchy table cannot express returns a
zero-row data.table rather than an error. A \code{from} or \code{to} value of
\code{"baregion"} always does this, because the bundled table carries no
BA-region codes. Use \code{\link[=nor_locations_names]{nor_locations_names()}} to list the BA-regions
themselves.
}
\examples{
csdata::nor_locations_hierarchy_from_to(from = "wardoslo", to = "county")

nrow(csdata::nor_locations_hierarchy_from_to(from = "municip", to = "county"))

csdata::nor_locations_hierarchy_from_to(
  from = c("municip", "county"),
  to   = "georegion",
  include_to_name = TRUE
)[1:5]

# baregion is accepted but the bundled hierarchy holds no BA-region codes
nrow(csdata::nor_locations_hierarchy_from_to(from = "municip", to = "baregion"))
}
\seealso{
\code{vignette("csdata", package = "csdata")}, which calls
\code{nor_locations_hierarchy_from_to()} on a worked example.
\code{vignette("locations_norway", package = "csdata")} tabulates the
\code{location_code} values returned by \code{\link[=nor_locations_names]{nor_locations_names()}}, which are the
values \code{from_code} and \code{to_code} are drawn from.
}
